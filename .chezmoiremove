{{/* Targets to delete from $HOME. Deleting a source file stops chezmoi managing
     it, but leaves the deployed copy behind — this removes it. */}}

{{/* Absorbed into productivity-summary: its garden is now study.rb's page. */}}
.claude/skills/get-moving

{{/* Mermaid renderer, dropped as unused. Takes node_modules with it. */}}
.local/share/mermaid-render

{{/* Replaced by the exhalekit plugin. rubycritic went with them: bin/diff-quality runs it. */}}
.claude/hooks/exhale-reminder.sh
.claude/skills/simplify-with-analysis
.claude/skills/rubycritic
